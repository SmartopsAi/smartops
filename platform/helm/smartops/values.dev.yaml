global:
  smartops:
    env: dev
    namespace: smartops-dev
    labels:
      project: smartops
      env: dev

# control subcharts through these flags (disabled in dev to avoid clash)
telemetry:
  prometheus:
    enabled: true
    prometheus-node-exporter:
      enabled: false   # <-- disable for Docker Desktop
  loki:
    enabled: false
  otel:
    enabled: false

grafana:
  adminPassword: "SmartOps@123"

# apps (placeholders — enabled later phases)
apps:
  erpSimulator:
    enabled: true
  orchestrator:
    enabled: false
  policyEngine:
    enabled: false
  agentDetect:
    enabled: false
  agentDiagnose:
    enabled: false

