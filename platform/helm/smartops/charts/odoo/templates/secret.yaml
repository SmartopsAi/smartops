{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: odoo-postgres-secret
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ .Values.postgres.password | quote }}
{{- end }}
